#include "main.h"

/**
 * _abs - code starts here
 *
 * @a: number to be checked
 *
 * Description: checks for absolute numbers
 *
 * Return: 1 if absolute and 0 if otherwise
 */
int _abs(int a)
{
	if (a >= 0)
	{
		return (a);
	}
	return (-a);
}
