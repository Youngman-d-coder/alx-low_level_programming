#include "main.h"

/**
 * main - Entry point
 *
 * Description: Tests if number is positive or negative
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    int c;

    c = 0;

    positive_or_negative(c);

    return (0);
}
