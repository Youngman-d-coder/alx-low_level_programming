#include "main.h"

/**
 * main - entry function
 * Description: Tests if Positive or Negative
 * Return: always 0(success)
 */
int main(void)
{
	int C;

	C = 0;
	positive_or_negative(C);
	return (0);
}
